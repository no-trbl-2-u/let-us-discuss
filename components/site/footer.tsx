export function Footer() {
  return (
    <footer className="border-t border-ink/10">
      <div className="mx-auto flex max-w-5xl flex-col gap-2 px-6 py-6 text-sm font-sans text-ink/70 md:flex-row md:items-center md:justify-between">
        <span>boardroom — pitch to spec.</span>
        <span>Phase 1 substrate. v1 in build.</span>
      </div>
    </footer>
  )
}
